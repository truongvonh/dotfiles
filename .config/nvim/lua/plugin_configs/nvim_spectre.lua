--require('spectre').setup
--require('spectre').open()

--local is_ok, spectre = require('spectre')
--print(spectre.state)
--print(vim.inspect(require('spectre')))
--print(spectre)
--require('spectre').search({
--search_query = 'new',
--replace_query = '',
--})
--
require('spectre').setup()

--require('windline').add_status(
--require('spectre.state_utils').status_line()
--)
